name: POWL runtime contract

on:
  pull_request:
    paths:
      - "powl/runtime/**"
      - "powl/objects/tagged_powl/**"
      - "tests/test_workflow_runner.py"
      - "pyproject.toml"
      - "requirements.txt"
      - ".github/workflows/runtime-contract.yml"
  push:
    branches: [main]
    paths:
      - "powl/runtime/**"
      - "powl/objects/tagged_powl/**"
      - "tests/test_workflow_runner.py"
      - "pyproject.toml"
      - "requirements.txt"
      - ".github/workflows/runtime-contract.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: powl-runtime-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  contract:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.11", "3.12", "3.13"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip
          cache-dependency-path: requirements.txt
      - run: python -m pip install --upgrade pip
      - run: python -m pip install -e . pytest
      - run: python -m pip check
      - run: python -m pytest tests/test_workflow_runner.py tests/test_powl_v2_research_contract.py
      - run: python -m compileall -q powl/runtime tests/test_workflow_runner.py
